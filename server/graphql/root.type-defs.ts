import { typeDefs as authentication } from './authentication/authentication.type-defs';
import { typeDefs as campaign } from './campaign/campaign.type-defs';

export const typeDefs = [
  authentication,
  campaign,
];