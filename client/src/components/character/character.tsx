import React, { ReactElement } from 'react';

// Style
import './character.scss';

const Character: React.FC = (): ReactElement => {
  return (
    <div className="character-container">
      <div className="character-layout">

      </div>
    </div>
  );
};

export default Character;
